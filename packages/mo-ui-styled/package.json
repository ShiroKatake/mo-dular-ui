{
  "name": "@modular-ui/styled",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "echo styled",
    "build": "rm -rf dist && mkdir dist && cd src && tsc --module commonjs && cp -r ./package.json ../dist"
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}
