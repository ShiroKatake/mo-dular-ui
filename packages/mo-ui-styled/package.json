{
  "name": "@modular-ui/styled",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "echo styled",
    "build": "rm -rf dist && mkdir dist && cd src && tsc --module commonjs && cd .. && cp ./package.json ./dist"
  },
  "peerDependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "styled-components": "^6.0.0"
  }
}
